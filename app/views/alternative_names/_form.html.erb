<%= form_for(@alternative_name) do |f| %>
  <% if @alternative_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alternative_name.errors.count, "error") %> prohibited this alternative_name from being saved:</h2>

      <ul>
      <% @alternative_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
